# Spam scoring actions.
# > 15 → reject outright
# > 8  → rewrite Subject (we don't, but flag); deliver to Spam folder
# > 4  → add headers (let the user/IMAP rules decide)
reject = 15;
add_header = 4;
greylist = null;     # disable greylisting; harmful UX for new users
